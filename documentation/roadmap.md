# Дорожная карта: Amber → VST3-ассистент

Рабочий документ, обновляется по мере продвижения. Это хелпер для личного
использования, не продукт — минимальный набор, который решает задачу, без
переусложнения. Оценки по срокам сознательно не даются — это side-проект,
темп не фиксирован.

## Главный архитектурный принцип: инструмент строго рекомендательный

Программа **никогда не трогает аудио сама**. Она измеряет, диагностирует и
говорит: «здесь, в этом месте, нужно вот это действие вот этим плагином» —
и всё. Применяет пользователь, в своей DAW, своими инструментами. Это
касается ВСЕХ категорий без исключения, включая то, что вычисляется
однозначно (гул, клиппинг) — даже там, где у программы есть готовый и
проверенный способ починить самой (notch-фильтр на гул уже реализован и
работает), продуктовое поведение — не применять, а сформулировать точную
рекомендацию: «на 0:23 нужен dehum на 50.3Гц, используй iZotope/что угодно
с notch-фильтром» или «здесь деклиппинг, вот отрезок». Готовая функция
устранения гула (`noise.remove_narrowband_hum`) остаётся в кодовой базе
как проверенная утилита (пригодится, если понадобится где-то ещё), но НЕ
вызывается ни автоматически, ни как часть продуктового потока — только
`refine_narrowband_freq` (уточнение частоты) используется, чтобы
рекомендация была точной, а не «где-то у 50».

Следствие: различие «без выбора / с выбором» (см. ниже) больше не о том,
чинит ли программа сама — она не чинит сама никогда. Оно о том, насколько
однозначен ответ и насколько ему можно доверять как единственно верному
(гул — да, наложение дублей — нет, там несколько художественно валидных
решений одного измерения).

## Общий принцип: никакой автоматической цепочки между стадиями

Блоки — это будущие «кнопки», не автоматический пайплайн. Каждая стадия
запускается явно, пользователем, шаг за шагом — не автоматически следом за
предыдущей. Никакой блокирующей логики не нужно (не «анализ недоступен,
пока...», просто естественный порядок шагов, который пользователь и так
соблюдает). Итерации определяет пользователь.

Отсюда: **на одну кнопку «Анализ» — полный список рекомендаций, какой
удалось посчитать, а не одна правка за раз**. Как часто гонять анализ
заново — целиком на усмотрение пользователя, не то, что программа
пытается оптимизировать за него; если итераций будет много — это нормально,
не повод урезать выдачу до одной правки.

## Порядок

Блоки 2-8 — чистый Python, работают в консоли на этой машине, **не требуют
VST3/Windows вообще**. Блоки 0-1 (захват из DAW) — единственные, что
завязаны на плагин, и делаются в самом конце, на второй машине (Windows),
когда весь остальной функционал уже готов и проверен на консоли.

```
2 (детект + точные параметры фикса, отдельная стадия/«кнопка», ничего не
   применяет сама)
  → 3 (наложение дублей: измерение здесь, рекомендация — в 8) → 4 → 5 → 6 → 7 → 8
    (каждая стадия — отдельный явный запуск пользователем, естественный
    порядок шагов, без блокирующей логики и без автозапуска следующей)
                              ↓
                          0 → 1   (машина на Windows, VST3)
```

## Ключевой принцип для Блока 8 (каталог плагинов): граница — «есть ли выбор»

Не о том, чинит ли программа сама (не чинит — никогда, см. выше). Настоящий
признак — есть ли у фикса альтернатива, или ответ однозначен.

- **Без выбора** (гул, деклиппинг, DC-офсет): параметры вычисляются точно —
  частота гула, добротность, сколько снять, где именно клиппинг. Любой
  подходящий плагин справится одинаково, рекомендация — не про вкус, а про
  наличие нужной функции у плагина. Можно мэтчить на конкретный плагин
  сразу после Блока 2, не дожидаясь симулятора и матрицы интерференции
  (Блок 6-7).
- **С выбором** (наложение дублей, все вкусовые EQ/динамика/пространство
  ходы): одно и то же измерение допускает разные художественные решения.
  Дубли разъехались на 3мс — можно свести в ноль (чисто), можно раздвинуть
  до 20мс (эффект хоруса), можно развести только по панораме и время не
  трогать вовсе. Это решение, не однозначное восстановление, и оно ждёт
  полный рекомендатель (Блок 7) с матрицей интерференции.

Отсюда: наложение дублей **измеряется** в Блоке 3 (`layering.py`), но
**рекомендация по фиксу** для него — часть Блока 8 «с выбором», не ранняя
часть.

**Панорама/стерео-позиционирование — сознательно вне скоупа Блоков 6-7.**
Сейчас работаем только с частотами (EQ/динамика/сатурация), пространственное
размещение по панораме пользователь берёт на себя в конце сам, по слуху —
это финальный творческий штрих, не то, что должен диктовать пресет.

## Чистка и анализ — раздельные стадии, без автоматической цепочки

Деклиппинг (когда будет реализован как рекомендация с точными параметрами)
описывает восстановление срезанных верхушек — то есть **подъём пиков** там,
где они упирались в 0.0 dBFS. Если пользователь применит его в своей DAW,
`true_peak_dbfs`, `sample_peak_dbfs`, `plr`, `crest_factor_db` изменятся — а
PLR сидит в пресете как зона. Сравнивать с пресетом метрики, посчитанные
ДО того, как пользователь фактически восстановил пики в своём проекте, —
значит сравнивать с ещё искалеченным сигналом.

Решение то же, что и общий принцип: не автоматика, а раздельные явные
стадии. Чистка (Блок 2, диагностика + рекомендации) и анализ (Блок 3 и
дальше) — отдельные шаги. Пользователь сам решает, когда он реально
применил рекомендации из чистки в своей DAW и готов запускать анализ
заново на обновлённом материале.

## Статус блоков

### Блок 2 — очистка/восстановление (без выбора — 8a целиком отсюда)
**Статус: в работе**

Диагностика + точные параметры для рекомендации. Никогда не применяет
сама — см. главный принцип выше.

- [x] Локализация клиппинга: `engine.detect_clipping` — список отрезков
      (таймкоды, доля трека, длина серий). Тесты
      `tests/synthetic/test_clipping_localization.py` (4/4), интегрировано
      в `write_report`. Дальше — формулировка как рекомендация («деклип на
      отрезках X-Y»), не встроенная репарация
- [x] Детекция гула + точная частота: `noise.find_persistent_narrowband`
      (детекция) + `noise.refine_narrowband_freq` (уточнение частоты
      параболической интерполяцией — детектор даёт точность STFT-бина
      ~11Гц, для точной рекомендации мало, доли Гц после уточнения).
      Подключено в диагностику `track_avg_metrics`/`write_report`.
      `noise.remove_narrowband_hum` — рабочая, протестированная функция,
      остаётся в кодовой базе как утилита, но НЕ часть продуктового потока
      (продукт только рекомендует, не применяет). Тесты
      `tests/synthetic/test_hum_removal.py` (4/4, тестируют саму
      способность вычислить и применить фикс — как доказательство, что
      параметры для рекомендации верны, не как продуктовую функцию)
- [x] Формат вывода рекомендации: `analysis/recommendations.py`
      (`Recommendation`, `restoration_recommendations`,
      `all_restoration_recommendations`) — превращает `clipping_detail`/
      `hum_candidates` в текст «[роль] место: категория — уверенность»,
      родовая категория (`declip`/`dehum`), не конкретный плагин (ждёт
      Блок 8). Двойная сортировка (таймлайн → уверенность) уже здесь, не
      отложена до Блока 7. Подключено в `write_report` (новый раздел
      «Рекомендации по очистке») и в `measurements.json`
      (`restoration_recommendations`). Тесты
      `tests/synthetic/test_recommendations.py` (6/6)
- [x] Деклиппинг: параметры репарации для текста рекомендации (не сама
      репарация). `detect_clipping` теперь возвращает на каждый отрезок
      `severity` (`click`/`sustained`, порог `CLICK_MAX_S`=5мс) и `channels`
      (какие именно каналы задеты, не просто «есть клиппинг»). Категория
      рекомендации разделена на `declip_click`/`declip_sustained` — разные
      режимы восстановления у реального инструмента (короткий щелчок vs
      затянутый клиппинг), Блок 8 будет матчить по этому тегу отдельно.
      Тесты обновлены (`test_clipping_localization.py` 5/5,
      `test_recommendations.py` 7/7)
- [ ] Два окна анализа: узкое (клики/клиппинг), среднее (гул/реверб-диагностика)
- [ ] Эхо/бликид vs осознанный реверб — экспериментально, без обещаний
      (в плагине закрывается тумблером «реверб натуральный» у пользователя,
      для консольного режима на готовых файлах остаётся открытым вопросом)

### Блок 3 — наложение дублей (измерение) + полный анализ
**Статус: не начат**
- [ ] Подключить `layering.py` (`analyze_pair`) к оркестратору — код готов,
      не вызывается. Это только измерение (расхождение по времени/питчу,
      риск гребёнки) — рекомендация по фиксу в Блоке 8 («с выбором»)
- [ ] Обобщить с пары дублей на N

### Блок 4 — частотные конфликты
**Статус: не начат**
- [ ] Подключить `masking_erb.analyze_group` к оркестратору — уже принимает
      произвольное число дорожек, просто интеграция

### Блок 5 — дельты с атрибуцией
**Статус: не начат**
- [ ] Для каждой метрики вне зоны — численная дельта до цели, не только
      статус
- [ ] Атрибуция по (роль, **секция**, не таймкод) — переиспользует уже
      посчитанные по-стемовые и по-секционные метрики. Скользящее окно не
      даёт чётких границ события — «в припевах ярче на 1.8дБ» честно,
      «с 1:23 до 1:27» — ложная точность, порождённая окном, а не реальной
      границей проблемы

### Блок 6 — симулятор вмешательств + матрица интерференции
**Статус: не начат**
- [ ] ~8-10 канонических ходов (пара-тройка колоколов, полки с обоих
      концов, один компрессор, одна сатурация) — не больше, расширять по
      факту нужды. Панорама/пространство сознательно не входит (см. выше)
- [ ] Прогон по корпусу **только быстрым путём метрик** (без
      `--deep-psychoacoustics`)
- [ ] Таблица «вмешательство → вектор дельт метрик», данные рядом с
      `amber.json`
- [ ] Калибровка против данных деконволюции Шага 5 (реальные вмешательства
      инженера сведения → реальные сдвиги метрик между версиями)

### Блок 7 — рекомендатель («с выбором»)
**Статус: не начат**

Единица выдачи — **полный список рекомендаций за один запуск анализа**, не
одна правка за раз (применение — руками, программа ничего не применяет
сама). Как часто перезапускать анализ — на усмотрение пользователя, не
предмет оптимизации со стороны программы.

Матрица интерференции (Блок 6) при этом остаётся ограниченно надёжной —
хороша для маленького кластера одновременных ходов, хуже для
экстраполяции через весь трек разом. Стадийность (уровни → вычитающий
EQ → компрессия → добавляющий EQ → пространство) — не гейт, который
что-то скрывает до следующего нажатия, а способ ГРУППИРОВАТЬ и честно
маркировать уверенность: рекомендации внутри одной стадии проверены друг
против друга матрицей — высокая уверенность в совместном эффекте;
рекомендации из разных стадий показываются вместе, но с пометкой, что
совместный эффект между стадиями не проверен так же строго (структурно
малый по построению классического порядка сведения, но не нулевой).

- [ ] Полный список за один запуск, сгруппированный по стадиям сведения, с
      пометкой уверенности «внутри стадии / между стадиями»
- [ ] Сортировка внутри вывода — **двойная**: сначала по позиции на
      таймлайне трека (по секциям, в порядке звучания), поверх — по
      приоритетности внутри той же области. Так рекомендации читаются
      линейно по ходу трека, а не вперемешку по важности
- [ ] Каждая рекомендация — конкретный плагин/категория (из Блока 8),
      конкретное действие, конкретная секция (не таймкод, см. Блок 5)
- [ ] Стадийность — **поле в `plugins.json`**, не логика в Python
- [ ] Наложение дублей (из Блока 3) — здесь же, художественный выбор, не
      однозначный фикс
- [ ] Честные неразрешимые конфликты (по аналогии с `no_zone`)
- [ ] Округлённый вывод («примерно 2дБ в районе 3кГц»), не ложная точность

### Блок 8 — каталог плагинов
**Статус: ждёт список плагинов от пользователя**
- [ ] Схема `presets/plugins.json`: теги по типу вмешательства, категории
      обработки, платформа/формат, стадия сведения (см. Блок 7),
      `reliability` самой связки «проблема→плагин»
- [ ] Часть «без выбора» (гул, деклиппинг, DC, наводки) — можно мэтчить
      сразу после Блока 2, не дожидаясь Блока 6-7; рекомендация всегда
      текстовая («используй Х для Y»), программа не применяет
- [ ] Часть «с выбором» (дубли, все вкусовые ходы) — ждёт Блок 6-7
- [ ] Матчинг — пересечение тегов + ранжирование, тот же принцип, что в
      `verdict.py`

### Блок 0 — спайк VST3 (Windows)
**Статус: не начат**
- [ ] Тулчейн: пустой плагин собирается и грузится
- [ ] Захват без щелчков (лок-фри кольцевой буфер → отдельный поток → диск)
- [ ] PDC-тест: два трека, синхронный захват, кросс-корреляция GCC-PHAT —
      решает, нужна ли доп. компенсация или sender-first-in-chain достаточно

### Блок 1 — инфраструктура захвата (Windows)
**Статус: не начат**
- [ ] Sender-плагин: имя, роль (дропдаун), тумблер «реверб натуральный»,
      индикатор захвата
- [ ] Состояние через `getState`/`setState`
- [ ] Темп/такт/позиция из `ProcessContext` (бесплатно)
- [ ] Мост к Python: WAV+JSON в watched-папке, демон — отдельный процесс
- [ ] GCC-PHAT поверх сырого захвата (страховка вне зависимости от исхода
      PDC-теста)
- [ ] Отчёт — HTML от демона в браузере, не C++ GUI
- [ ] Разделение быстрый/глубокий прогон — сохранить и здесь
