# Основные вычисления
numpy>=2.0
scipy>=1.14
pandas>=2.2

# Ввод-вывод и ресемплинг аудио
soundfile>=0.13
librosa>=0.10        # pYIN, LPC, STFT-обвязка

# Громкость по стандарту EBU R128 / ITU-R BS.1770
pyloudnorm>=0.2

# Психоакустика (ISO 532-1 loudness, DIN 45692 sharpness, roughness)
mosqito>=1.2

# Разделение источников (Demucs, htdemucs_ft) — режим "целый трек"
# Тянет за собой torch/torchaudio — тяжёлая зависимость, но нужна только
# для режима 1 (Demucs). Режим 2 (трек-аут с реальными дорожками) работает
# без Demucs вообще.
demucs>=4.0
torch>=2.0

# Тесты (не нужны для запуска оркестратора, только для Project/tests/)
pytest>=8.0
